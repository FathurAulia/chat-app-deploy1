{
  "scripts": {
    "install-server": "cd server && yarn",
    "install-public": "cd server && npm install",
    "start": "node server/index.js",
    "public": "yarn start --prefix public"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
