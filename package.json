{
  "scripts": {
    "install": "cd public && yarn && cd ../server && yarn",
    "start": "node server/index.js",
    "public": "yarn start --prefix public",
    "dev": "concurrently \"npm start\" \"npm run public\""
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
